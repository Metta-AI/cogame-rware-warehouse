# CI for a Coworld game repo (Metta-AI/cogame-rware-warehouse).
#
# Goes to:  .github/workflows/ci.yml  in the coworld repo.
# Substitute: rware-warehouse  (lowercase game slug, e.g. bullwhip)
#             coworld-rware-warehouse (local image name WITHOUT a tag, must match the image in
#                      compose.yaml minus ":latest", e.g. coworld-bullwhip)
#             4 (seat count -- a CROSS-CHECK, not a fallback:
#                      docker_smoke.sh takes the seat count solely from
#                      certification.game_config.num_agents and hard-fails if
#                      this value disagrees with it)
#
# The coworld-builder sandbox has no docker, no nim, no emsdk and no browser:
# this workflow is the ONLY place those toolchains run. Phase 20's exit
# criterion is this workflow green on main.
#
# It also needs tools/ci/viewer_smoke.mjs (copied verbatim from coworld-builder
# templates/tools/ci/; no substitutions) -- the wasm-viewer job opens the built
# bundle in headless chromium and fails if the replay never renders.
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  SLUG: rware-warehouse
  IMAGE: coworld-rware-warehouse
  # Pins mirror the Dockerfile build stage; bump both together.
  NIMBY_VERSION: "0.1.26"
  NIM_VERSION: "2.2.4"

jobs:
  # ---------------------------------------------------------------------
  # Native Nim tests. Same nimby recipe as the Dockerfile build stage, so a
  # green test job means the image's compiler and package tree agree.
  # ---------------------------------------------------------------------
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5

      - name: Cache nimby toolchain and nimcache
        uses: actions/cache@v4
        with:
          path: |
            ~/.nimby
            ~/.cache/nim
          key: nimby-${{ runner.os }}-${{ env.NIMBY_VERSION }}-${{ env.NIM_VERSION }}-${{ hashFiles('nimby.lock') }}
          restore-keys: |
            nimby-${{ runner.os }}-${{ env.NIMBY_VERSION }}-${{ env.NIM_VERSION }}-

      - name: Install nimby and Nim
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.local/bin"
          arch="$(dpkg --print-architecture)"
          case "$arch" in
            amd64) asset="nimby-Linux-X64" ;;
            arm64) asset="nimby-Linux-ARM64" ;;
            *) echo "::error::unsupported runner arch: $arch"; exit 1 ;;
          esac
          curl -fsSL -o "$HOME/.local/bin/nimby" \
            "https://github.com/treeform/nimby/releases/download/${NIMBY_VERSION}/${asset}"
          chmod +x "$HOME/.local/bin/nimby"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          echo "$HOME/.nimby/nim/bin" >> "$GITHUB_PATH"
          "$HOME/.local/bin/nimby" use "$NIM_VERSION"

      - name: Sync Nim dependencies
        run: |
          set -euo pipefail
          if [ -f nimby.lock ]; then
            nimby --global sync nimby.lock
          else
            echo "no nimby.lock in the repo; skipping dependency sync"
          fi

      - name: Regenerate nim.cfg from the synced package tree
        # The committed nim.cfg pins the AUTHOR's machine package paths. Rebuild
        # it from this runner's ~/.nimby/pkgs, exactly as the Dockerfile does.
        run: |
          set -euo pipefail
          rm -f nim.cfg
          for pkg in "$HOME"/.nimby/pkgs/*; do
            [ -e "$pkg" ] || continue
            if [ -d "$pkg/src" ]; then
              echo "--path:\"$pkg/src\"" >> nim.cfg
            else
              echo "--path:\"$pkg\"" >> nim.cfg
            fi
          done
          echo '--path:"src"' >> nim.cfg
          echo "::group::nim.cfg"
          cat nim.cfg
          echo "::endgroup::"

      - name: Run tests
        # Every tests/*.nim runs twice: debug (range/overflow checks, stack
        # traces) and -d:release (release-only compile errors and codegen bugs
        # that debug runs never see). Repo variables narrow that:
        #   NIM_TESTS               space-separated test files (default tests/*.nim)
        #   NIM_TESTS_DEBUG_ONLY    files to run ONLY in debug
        #   NIM_TESTS_RELEASE_ONLY  files to run ONLY in release
        env:
          NIM_TESTS: ${{ vars.NIM_TESTS }}
          NIM_TESTS_DEBUG_ONLY: ${{ vars.NIM_TESTS_DEBUG_ONLY }}
          NIM_TESTS_RELEASE_ONLY: ${{ vars.NIM_TESTS_RELEASE_ONLY }}
        run: |
          set -uo pipefail
          tests="${NIM_TESTS:-}"
          if [ -z "${tests}" ]; then
            tests="$(ls tests/*.nim 2>/dev/null | tr '\n' ' ')"
          fi
          if [ -z "$(echo "${tests}" | tr -d ' ')" ]; then
            echo "::error::no test files found (tests/*.nim empty and NIM_TESTS unset)"
            exit 1
          fi
          fail=0
          for t in ${tests}; do
            if [ ! -f "${t}" ]; then
              echo "::error::test file not found: ${t}"
              fail=1
              continue
            fi
            modes="debug release"
            case " ${NIM_TESTS_DEBUG_ONLY} " in *" ${t} "*) modes="debug" ;; esac
            case " ${NIM_TESTS_RELEASE_ONLY} " in *" ${t} "*) modes="release" ;; esac
            for m in ${modes}; do
              if [ "${m}" = "release" ]; then
                flags="--hints:off -d:release"
              else
                flags="--hints:off"
              fi
              echo "::group::nim r ${flags} --path:src ${t}"
              # shellcheck disable=SC2086
              if ! nim r ${flags} --path:src "${t}"; then
                echo "::error::FAILED (${m}) ${t}"
                fail=1
              fi
              echo "::endgroup::"
            done
          done
          exit "${fail}"

      - name: The replay forensics tools still build and read the fixture
        # tools/expand_replay.nim and tools/extract_events.nim import the sim
        # module, so a rename in src/ that nothing else references still breaks
        # them -- and they are what a prod incident is read with. Compile both
        # and re-derive the committed fixture's event stream.
        run: |
          set -euo pipefail
          nim r --hints:off -d:release --path:src tools/extract_events.nim \
            tests/replays/rware.replay
          nim r --hints:off -d:release --path:src tools/expand_replay.nim \
            tests/replays/rware.replay --ticks 4 --board 120

      - name: The broadcast page is the starter's page plus the game block
        # tools/build_broadcast_page.py IS the provenance record for the fork:
        # it takes coworld-ctf's page byte for byte, deletes exactly the
        # elements the design note removes and appends the game block. CI has
        # no starter checkout, so what is asserted here is that the committed
        # page still contains the inherited half and the splice marker exactly
        # once -- tests/test_rware_viewer.nim does the rest.
        run: |
          set -euo pipefail
          python3 - <<'PY'
          page = open('client/replay_broadcast.html', encoding='utf-8').read()
          marker = "RWARE-WAREHOUSE additions to the inherited coworld-ctf chrome"
          assert page.count(marker) == 1, page.count(marker)
          head = page[:page.index(marker)]
          for needle in ('<!DOCTYPE html>', '<!-- WIRE_CONSTANTS -->',
                         '<!-- CHROME_COMMON -->', 'id="transport"',
                         'function relayout()'):
              assert needle in head, needle
          print('broadcast page ok:', len(page), 'bytes')
          PY

      - name: The shipped baseline pick is still the swept pick
        # The design note says CI re-runs the tuning harness. It does: the same
        # 27-cell head-to-head grid the pick came from, asserting
        # tools/ci/baseline_tuning.json equals DefaultBaselineParams and that
        # the pick still ranks in the top half. tests/test_rware_tuning.nim
        # covers the sweep over a shorter horizon; this is the tool itself, at
        # the horizon it was tuned at, so a controller change that invalidates
        # the pick is red here rather than in a ladder round.
        run: |
          set -euo pipefail
          nim r --hints:off -d:release --path:src tools/tune_baselines.nim --check

  # ---------------------------------------------------------------------
  # The manifest under the INSTALLED coworld CLI's own loader, plus the
  # scaffold-residue and executable-bit gates. Repo CI otherwise cannot see a
  # manifest the platform validator rejects: 0.1.42+ wants game.replay_viewer
  # (not top-level), no top-level version, no game.display_name, game.owner
  # required, and no runner-managed `tokens` in any game_config
  # (collab-cooking, 2026-08-25).
  # ---------------------------------------------------------------------
  manifest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      COWORLD_PKG: "coworld[auth]==0.1.43"
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v5

      - name: No scaffold placeholder survived substitution
        run: ./tools/ci/check_placeholders.sh

      - name: Both committed scripts are executable
        run: |
          set -euo pipefail
          test -x tools/ci/docker_smoke.sh
          test -x tools/build_replay_viewer.sh
          echo "exec bits ok"

      - name: The manifest loads under the installed coworld CLI
        run: |
          set -euo pipefail
          uvx --from "$COWORLD_PKG" python - <<'PY'
          import json, pathlib, sys
          template = json.loads(
              pathlib.Path("coworld_manifest_template.json").read_text())
          # The placeholder the compose service name derives; `coworld build`
          # substitutes it, so substitute it here too before validating.
          text = json.dumps(template).replace(
              "{{RWARE_WAREHOUSE_IMAGE}}", "coworld-rware-warehouse:latest")
          manifest = json.loads(text)
          # `coworld build --version X` injects game.version, so a TEMPLATE
          # manifest has none and the validator's "game.version Field required"
          # would be an artefact of validating the wrong artifact. Inject the
          # same field the build does.
          manifest["game"]["version"] = "0.1.0"
          from coworld import manifest as coworld_manifest
          errors = []
          for name in ("validate_upload_manifest", "_load_template_manifest",
                       "load_manifest"):
              fn = getattr(coworld_manifest, name, None)
              if fn is None:
                  continue
              try:
                  fn(manifest)
                  print(f"{name}: ok")
              except TypeError:
                  print(f"{name}: not callable with a dict, skipped")
              except Exception as error:      # noqa: BLE001
                  errors.append(f"{name}: {error}")
          if errors:
              print("\n".join(errors), file=sys.stderr)
              raise SystemExit(1)
          PY

  # ---------------------------------------------------------------------
  # Packaging smoke: the production image builds, and one real episode runs
  # end to end in raw docker with the certification fixture's seat mix.
  # ---------------------------------------------------------------------
  docker-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
      - uses: docker/setup-buildx-action@v3

      # Invoked by path, not through `bash`: a `bash SCRIPT` call ignores the exec
      # bit, so a 0644 helper would go green here and only fail wherever it is
      # executed directly.
      - name: Assert the smoke script is present and executable
        run: |
          set -euo pipefail
          test -f tools/ci/docker_smoke.sh || {
            echo "::error::tools/ci/docker_smoke.sh is missing"; exit 1; }
          test -x tools/ci/docker_smoke.sh || {
            echo "::error::tools/ci/docker_smoke.sh must be committed executable (mode 100755)."
            echo "::error::Fix with: git update-index --chmod=+x tools/ci/docker_smoke.sh"
            exit 1; }

      - name: Build image
        run: docker build --platform=linux/amd64 -t "${IMAGE}:ci" .

      - name: Raw-Docker episode smoke
        # SMOKE_SLUG is passed explicitly so the workflow's SLUG is the single
        # source for the game/player entrypoint names; the script's own
        # scaffold-substituted default is only a fallback.
        env:
          SMOKE_SLUG: ${{ env.SLUG }}
          # The replay is the starter's BINARY COWLDRWH format -- the static
          # wasm viewer parses exactly this, and a JSON replay would mean
          # rewriting replays.nim, replay_runtime.nim, static_replay_worker.js
          # and wasm_replay_smoke.cjs, i.e. the machinery this fork exists to
          # reuse. tools/replay_summary.py is the strict-UTF-8 JSON view of the
          # same bytes (Python 3 stdlib only) and CI runs it below.
          SMOKE_REQUIRE_REPLAY_JSON: "0"
          SMOKE_REPLAY_OUT: dist/smoke/episode.replay
        run: ./tools/ci/docker_smoke.sh "${IMAGE}:ci"

      # The strict-UTF-8 JSON view of the bytes the smoke just produced. This
      # is the phase-60 "replay parses under a strict parser" check, run on
      # every push rather than once at release.
      - name: Replay summary parses as strict UTF-8 JSON
        run: |
          set -euo pipefail
          python3 tools/replay_summary.py dist/smoke/episode.replay \
            > dist/smoke/summary.json
          python3 - <<'PY'
          import json
          raw = open('dist/smoke/summary.json', 'rb').read()
          raw.decode('utf-8')            # strict: no lone surrogates, no
          summary = json.loads(raw)      # byte-truncated codepoints
          assert summary['protocol'] == 'rware-warehouse/v1', summary['protocol']
          assert summary['results']['reason'] in ('complete', 'deadline'), summary
          assert summary['tickCount'] > 0, summary
          assert len(summary['directives']) > 0, summary
          assert len(summary['orders']) > 0, summary
          print('replay summary ok:', summary['protocol'],
                summary['results']['reason'], summary['tickCount'], 'frames')
          PY

      # The replay this episode produced is the ONLY replay in CI that is
      # known to be a real, current-format episode of this game. wasm-viewer
      # downloads it and loads it in a browser, so the viewer is tested
      # against the bytes the platform will actually serve rather than a
      # hand-written fixture that can drift.
      - name: Upload the smoke replay
        uses: actions/upload-artifact@v4
        with:
          name: smoke-replay
          path: dist/smoke/
          if-no-files-found: error
          retention-days: 14

  # ---------------------------------------------------------------------
  # The static replay viewer. The platform serves THIS bundle from
  # /v2/coworlds/replays/static/<cow_id>/<sha>/index.html, so a bundle that
  # does not build (or is missing its wasm) means every hosted replay hangs.
  # tools/build_replay_viewer.sh falls back to the pinned emscripten/emsdk
  # container (Dockerfile.replay-viewer) because the runner has no emcc.
  # ---------------------------------------------------------------------
  wasm-viewer:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    # The bundle is EXECUTED here, not merely built, and it is executed against
    # the replay docker-smoke just produced -- hence the dependency.
    needs: docker-smoke
    steps:
      - uses: actions/checkout@v5
      - uses: docker/setup-buildx-action@v3

      # `coworld build` (bundle.py) refuses to package a source replay-viewer
      # bundle unless the hook is BOTH a file and os.X_OK:
      #   "Coworld builds with a source replay viewer bundle require an
      #    executable build hook".
      # Running it as `bash tools/build_replay_viewer.sh` would ignore the exec
      # bit entirely, so a mode-0644 hook passed CI and only blew up later, in
      # coworld-release.yml's "Build the Coworld manifest". Assert the bit, then
      # invoke by path so CI exercises exactly what `coworld build` requires.
      - name: Assert the replay-viewer build hook is present and executable
        run: |
          set -euo pipefail
          test -f tools/build_replay_viewer.sh || {
            echo "::error::tools/build_replay_viewer.sh is missing; coworld build needs it"
            echo "::error::to emit the static replay bundle"
            exit 1; }
          test -x tools/build_replay_viewer.sh || {
            echo "::error::tools/build_replay_viewer.sh must be committed executable (mode 100755):"
            echo "::error::coworld build hard-requires os.X_OK on the build hook."
            echo "::error::Fix with: git update-index --chmod=+x tools/build_replay_viewer.sh"
            exit 1; }

      - name: Assert the viewer load test is present
        run: |
          set -euo pipefail
          test -s tools/ci/viewer_smoke.mjs || {
            echo "::error::tools/ci/viewer_smoke.mjs is missing. It is the only gate that"
            echo "::error::EXECUTES the replay viewer; without it a bundle that deadlocks"
            echo "::error::(cogame-lantern, 2026-08-23) ships green. Copy it from"
            echo "::error::coworld-builder templates/tools/ci/viewer_smoke.mjs."
            exit 1; }

      - name: Build the static replay viewer bundle
        run: ./tools/build_replay_viewer.sh "$PWD/dist/static-replay-viewer"

      - name: Assert the bundle is complete
        run: |
          set -euo pipefail
          out="$PWD/dist/static-replay-viewer"
          test -s "${out}/index.html" || {
            echo "::error::${out}/index.html missing or empty"; exit 1; }
          wasm_count="$(find "${out}" -name '*.wasm' -size +1c | wc -l | tr -d ' ')"
          if [ "${wasm_count}" -lt 1 ]; then
            echo "::error::no non-empty .wasm module in ${out}"
            find "${out}" -type f | sed 's/^/  /'
            exit 1
          fi
          echo "bundle OK:"
          find "${out}" -type f -printf '  %p (%s bytes)\n'

      # -----------------------------------------------------------------
      # The load test. Everything above only proves files EXIST. cogame-lantern
      # shipped a viewer whose emscripten link flags (MODULARIZE / EXPORT_NAME)
      # and whose JS bootstrap came from two different starters: the factory was
      # never called, no error was thrown, and softmax.com sat on
      # "Loading replay..." forever with every file present and 200. Only
      # opening the page in a browser sees that.
      #
      # Playwright is pinned to 1.55.0 in BOTH places -- the npm module and the
      # browser download. Bump them together or the browser 404s.
      # -----------------------------------------------------------------
      - name: Download the smoke replay
        uses: actions/download-artifact@v4
        with:
          name: smoke-replay
          path: dist/smoke

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Playwright (pinned 1.55.0)
        run: |
          set -euo pipefail
          npm install --no-save playwright@1.55.0
          npx --yes playwright@1.55.0 install --with-deps chromium

      - name: Load the bundle in a real browser
        run: |
          set -euo pipefail
          # A for-loop, not `ls … | head -1`: under `set -o pipefail` an unmatched
          # glob makes ls exit 2 and kills the step before the harness ever runs
          # (cogame-raid 2026-08-23, run 32623414696).
          replay=""
          for candidate in dist/smoke/*.replay dist/smoke/replay.json; do
            if [ -f "${candidate}" ]; then replay="${candidate}"; break; fi
          done
          test -n "${replay}" || {
            echo "::error::docker-smoke uploaded no replay to dist/smoke"; ls -la dist/smoke || true; exit 1; }
          echo "loading ${replay} in dist/static-replay-viewer"
          # --strict-text-bounds fails the job if the viewer drew any text
          # outside its canvas -- a bubble, caption or nameplate with nowhere
          # to go, invisible to the load signal and to the screenshot
          # (cogchemists 2026-08-24: four speech bubbles drawn at a negative y
          # and clipped to slivers). Keep it for a FIXED arena, which is every
          # coworld whose whole board fits the frame. DROP THIS FLAG ONLY if
          # the board is pannable, where drawing off-frame is normal -- the
          # count is still reported on stdout and in viewer-smoke.json.
          # --soak 10 is the OTHER half of the gate: loading is not playing.
          # cogball 0.1.4 loaded, drew one frame, set data-replay-loaded and
          # then threw inside that frame's feed render -- the adapter latched
          # `failed`, dropped every later Worker message and sat frozen looking
          # entirely healthy. The soak reads the clock/tick/scorebug three times
          # and fails unless the LAST interval still moved. Playback is one
          # sim tick per animation frame at 30 fps
          # (replay_runtime.TicksPerSecondBase), so the CI replay's ~500 frames
          # play for ~16.7 s and 10 s of soak lands mid-play.
          #
          # THIS STEP REPORTS `canvas text: 0 drawn`, AND THAT IS EXPECTED
          # HERE. The shipped shell transfers the board canvas to a Dedicated
          # Worker (replay-viewer/static_replay.js: transferControlToOffscreen)
          # and viewer_smoke.mjs hooks window.CanvasRenderingContext2D, which
          # does not reach Worker globals. Checklist item 15 is explicit that
          # `total: 0` is evidence of nothing, so the canvas-text bound gate for
          # this repo is the NEXT-BUT-ONE step, "Worst-case renderer fixture":
          # it runs the SAME client/broadcast_core.js on a main-thread canvas
          # with a worst-case frame at 360/620/630/1024 px and fails if the
          # board renderer drew no strings at all. Read its `canvas text:` line,
          # not this one.
          node tools/ci/viewer_smoke.mjs \
            --bundle dist/static-replay-viewer \
            --replay "${replay}" \
            --timeout 90 \
            --soak 10 \
            --strict-text-bounds

      # docker_smoke.sh runs with NO ANTHROPIC_API_KEY, so every seat in the CI
      # replay plays scripted and a scripted commander emits no `say` at all --
      # the smoke replay can never exercise the feed's text path (the
      # cogchemists 2026-08-24 scar). The fixture LOADS THE SHIPPED
      # dist/static-replay-viewer/index.html in an iframe and shims only the
      # wasm entry -- it does not re-implement the drawing (the particle-worlds
      # 2026-08-26 scar) -- driving the real page with a full-cap 120-rune
      # `say` on both seats at several canvas widths including 360 px.
      # It also drives the shipped client/broadcast_core.js against a plain
      # MAIN-THREAD canvas with that same frame, so the board's own strings
      # (the W1/W2 pad labels, the requested-shelf ids over the crates) are
      # drawn where viewer_smoke.mjs's canvas-text hook can measure them --
      # the bundle itself draws them inside a Worker, where the hook cannot
      # reach. THIS step's `canvas text:` line is the repo's never_inside
      # gate, and the fixture fails outright if that renderer drew no text.
      # wasm32-only failures (integer traps, a bad free through -d:useMalloc,
      # address-space exhaustion) are invisible to the native shards, and the
      # browser smoke only proves the page draws. Run the EXACT emitted module
      # headless under Node against the committed fixture too.
      - name: Headless wasm smoke of the emitted module
        run: |
          set -euo pipefail
          node tools/wasm_replay_smoke.cjs \
            dist/static-replay-viewer tests/replays/rware.replay 400

      - name: Worst-case renderer fixture (full-cap commander lines)
        run: |
          set -euo pipefail
          cp tools/ci/renderer_fixture.html dist/static-replay-viewer/
          cp dist/smoke/episode.replay dist/static-replay-viewer/
          mkdir -p fixture-out
          python3 -m http.server 8099 \
            --directory dist/static-replay-viewer >/dev/null 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid" 2>/dev/null || true' EXIT
          for _ in $(seq 1 40); do
            curl -fsS "http://127.0.0.1:8099/renderer_fixture.html" \
              -o /dev/null && break
            sleep 0.5
          done
          node tools/ci/viewer_smoke.mjs \
            --url "http://127.0.0.1:8099/renderer_fixture.html?replay=http://127.0.0.1:8099/episode.replay" \
            --timeout 90 \
            --strict-text-bounds \
            --out fixture-out

      - name: Upload the viewer smoke evidence
        # Always: the screenshot and the JSON are most useful when the job is red.
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: viewer-smoke
          path: |
            viewer-smoke.png
            viewer-smoke.json
          if-no-files-found: warn
          retention-days: 14

      - name: Upload the bundle
        uses: actions/upload-artifact@v4
        with:
          name: static-replay-viewer
          path: dist/static-replay-viewer
          if-no-files-found: error
          retention-days: 14
